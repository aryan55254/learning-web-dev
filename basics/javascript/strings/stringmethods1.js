// string methods are in built functions to manipulate a string
let x = "aryan";
let y = x.toUpperCase();
console.log(y);
//strings are originally immutable
//str.trim() removes whitespace
//concat add two strings can be done by just +