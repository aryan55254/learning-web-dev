/*const divs = document.querySelectorAll('div');

// Add a click event listener to each div
divs.forEach(div => {
    div.addEventListener('click', () => {
        div.remove(); // Remove the clicked div
    });
});*/

const x = document.getElementById("1");
x.addEventListener("click", () => x.remove());

const y = document.getElementById("2");
y.addEventListener("click", () => y.remove());

const w = document.getElementById("3");
w.addEventListener("click", () => w.remove());

const z = document.getElementById("4");
z.addEventListener("click", () => z.remove());


